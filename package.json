{
  "name": "ordercat-firebase-functions",
  "version": "1.0.0",
  "description": "Firebase Functions für OrderCat - Order Distribution Service",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@sparticuz/chromium": "^131.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "pdf-lib": "^1.17.1",
    "puppeteer-core": "^21.0.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/puppeteer": "^7.0.4",
    "@types/uuid": "^9.0.8",
    "typescript": "^5.3.0"
  },
  "private": true
}

